---
description: Run a task through lazy-dev. Decomposes, dispatches specialists, verifies, merges.
argument-hint: <describe the task you want done>
---

You have been invoked via `/run`. User's task:

$ARGUMENTS

Follow the instructions in `${CLAUDE_PLUGIN_ROOT}/agents/wrangler.md`. Start by calling `mcp__lazy-dev__create_run` with the full user brief.
